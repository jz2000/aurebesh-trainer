define(['app'], function(app) {
    "use strict";

describe("main application", function () {
    var sut;

    beforeEach(function () {
        
    });

//    beforeEach(inject(function ($controller, $rootScope) {
//        sut = $rootScope.$new();
//        $controller("UsersController", {
//            $scope: sut,
//            DataModelService: mockDataModelService
//        });
//    }));
    
    it("should restore lettersPool", function () {
        //resetStatistics();
        //expect(lettersPool.length).toBe(letters.length);
        expect(10).toBe(10);
    });

});

});